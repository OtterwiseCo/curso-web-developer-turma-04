const test = "12";

const person = {
  name: "joao",
  age: [],
};

// Valores que são considerados falsos no js: null, undefined, false, "", 0

if (person.age.length) {
  console.log(person.age);
}

// console.log(typeof test);
// console.log(typeof Number(test));
// if (Number(test)) {
//   console.log("valor é verdadeiro");
// }
